Project 10: Compiler I - Syntax Analysis

http://www.nand2tetris.org/course.php
http://www.nand2tetris.org/10.php

* Jack compiler, syntax analyzer: tokenizer and parser. 
* Application that parses Jack programs according to the Jack grammar, producing an XML file that renders the program's structure using marked-up text. 
* In the next project, the logic that generates the XML output will be morphed into logic that generates VM code.
* Implemented in Java.
